import React from "react";

const UserPage = async () => {
  // const sesstion = await getServerSession(authOptions);
  // console.log("session", sesstion);

  return <div>로그인 된 유저만 볼 수 있는 페이지입니다.</div>;
};

export default UserPage;
